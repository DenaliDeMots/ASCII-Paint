import React from 'react';
//This component represents the tool pallet where
//the user can select drawing settings like color etc.

const Pallet = (props) => (
    <div>

    </div>
)

export default Pallet;