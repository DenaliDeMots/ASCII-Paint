import {connect} from 'react-redux';



const mapStateToProps = (state, ownProps) => {
    return {
        key: TODO, //Identifying key
        character: TODO, //ASCII character
        style: TODO //font style info
    }
}

const mapDispatchToProps = (dispatch) => ({
    onMouseMove: (e) => {
        dispatch(actions.)
    }
 })

 TODO = undefined